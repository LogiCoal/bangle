
name: winx64
run-name: winx64 build
on: [push]
jobs:
  check-bats-version:
    runs-on: windows-2022
    steps:
      - name: pull code 
        run: git pull https://github.com/google/angle
      - name: Download a file
        run: curl https://storage.googleapis.com/chrome-infra/depot_tools.zip -o c:/tmp/
